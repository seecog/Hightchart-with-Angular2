import { YourIfDirective } from './your-if.directive';

describe('YourIfDirective', () => {
  it('should create an instance', () => {
    const directive = new YourIfDirective();
    expect(directive).toBeTruthy();
  });
});
