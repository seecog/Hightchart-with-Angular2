import { YourloopDirective } from './yourloop.directive';

describe('YourloopDirective', () => {
  it('should create an instance', () => {
    const directive = new YourloopDirective();
    expect(directive).toBeTruthy();
  });
});
