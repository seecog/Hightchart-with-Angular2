import { YourdirDirective } from './yourdir.directive';

describe('YourdirDirective', () => {
  it('should create an instance', () => {
    const directive = new YourdirDirective();
    expect(directive).toBeTruthy();
  });
});
