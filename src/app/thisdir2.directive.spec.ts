import { Thisdir2Directive } from './thisdir2.directive';

describe('Thisdir2Directive', () => {
  it('should create an instance', () => {
    const directive = new Thisdir2Directive();
    expect(directive).toBeTruthy();
  });
});
