import { ThisdirDirective } from './thisdir.directive';

describe('ThisdirDirective', () => {
  it('should create an instance', () => {
    const directive = new ThisdirDirective();
    expect(directive).toBeTruthy();
  });
});
