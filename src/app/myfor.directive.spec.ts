import { MyforDirective } from './myfor.directive';

describe('MyforDirective', () => {
  it('should create an instance', () => {
    const directive = new MyforDirective();
    expect(directive).toBeTruthy();
  });
});
